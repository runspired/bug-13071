import { complexHelper } from 'bug-13071/helpers/complex-helper';
import { module, test } from 'qunit';

module('Unit | Helper | complex helper');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = complexHelper([42]);
  assert.ok(result);
});
