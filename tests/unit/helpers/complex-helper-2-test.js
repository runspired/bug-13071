import { complexHelper2 } from 'bug-13071/helpers/complex-helper-2';
import { module, test } from 'qunit';

module('Unit | Helper | complex helper 2');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = complexHelper2([42]);
  assert.ok(result);
});
