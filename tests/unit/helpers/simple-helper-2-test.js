import { simpleHelper2 } from 'bug-13071/helpers/simple-helper-2';
import { module, test } from 'qunit';

module('Unit | Helper | simple helper 2');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = simpleHelper2([42]);
  assert.ok(result);
});
