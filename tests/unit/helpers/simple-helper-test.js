import { simpleHelper } from 'bug-13071/helpers/simple-helper';
import { module, test } from 'qunit';

module('Unit | Helper | simple helper');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = simpleHelper([42]);
  assert.ok(result);
});
